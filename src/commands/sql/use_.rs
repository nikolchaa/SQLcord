// /sql use <name>

use std::error::Error;

pub fn register() -> Result<(), Box<dyn Error>> {
	// placeholder registration helper (no-op)
	Ok(())
}

pub async fn run() -> Result<(), Box<dyn Error>> {
	// placeholder runtime handler (no-op)
	Ok(())
}
